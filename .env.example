# 复制为 `.env` 或供本地构建时注入。anon key 为公开、可进扩展包，勿把 service_role 放进扩展。
# Plasmo 会将以下变量打进前端（以 PLASMO_PUBLIC_ 为前缀）。

# 项目设置 → API：Project URL
PLASMO_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co

# 项目设置 → API：anon public
PLASMO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 登录 / Magic link：在 Supabase → Authentication → URL configuration
# 的 Redirect URLs 中新增（扩展 ID 因安装而异，在 chrome://extensions 展开本扩展可查看）：
#   chrome-extension://<你的扩展ID>/options.html
# 同一页也可把 Site URL 设为该 chrome-extension 地址，便于仅扩展内登录。
